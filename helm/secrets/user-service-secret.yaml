apiVersion: v1
kind: Secret
metadata:
  name: user-service-secret
  namespace: library-e2e-dev
type: Opaque
stringData:
  # No MongoDB credentials needed - using simple URI
  JWT_SECRET: "change-me-in-production-jwt-secret"
