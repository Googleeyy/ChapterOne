# GatewayClass is already created by kGateway installation
# Use: kubectl get gatewayclass kgateway

# Gateway - the actual ingress point for traffic
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: library-e2e-gateway
  namespace: chapterone
  labels:
    app: library-e2e
    component: gateway
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same

---

# HTTPRoute - routes traffic to frontend
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frontend-route
  namespace: chapterone
  labels:
    app: library-e2e
    component: gateway
spec:
  parentRefs:
    - name: library-e2e-gateway
  rules:
    # Route / (root) to frontend
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: frontend
          namespace: chapterone
          port: 80

---

# HTTPRoute - routes traffic to API services
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: api-route
  namespace: chapterone
  labels:
    app: library-e2e
    component: gateway
spec:
  parentRefs:
    - name: library-e2e-gateway
  rules:
    # Route /api/books/* to book-service (strip /api prefix)
    - matches:
        - path:
            type: PathPrefix
            value: /api/books
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: book-service
          namespace: chapterone
          port: 8081
    # Route /api/users/* to user-service
    - matches:
        - path:
            type: PathPrefix
            value: /api/users
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: user-service
          namespace: chapterone
          port: 8082
    # Route /api/borrows/* to borrow-service
    - matches:
        - path:
            type: PathPrefix
            value: /api/borrows
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: borrow-service
          namespace: chapterone
          port: 8083
    # Route /health to book-service for health checks
    - matches:
        - path:
            type: Exact
            value: /health
      backendRefs:
        - name: book-service
          namespace: chapterone
          port: 8081
